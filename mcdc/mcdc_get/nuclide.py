# The following is automatically generated by code_factory.py

from numba import njit


@njit
def neutron_xs_energy_grid(index, nuclide, data):
    offset = nuclide["neutron_xs_energy_grid_offset"]
    return data[offset + index]


@njit
def neutron_xs_energy_grid_all(nuclide, data):
    start = nuclide["neutron_xs_energy_grid_offset"]
    size = nuclide["neutron_xs_energy_grid_length"]
    end = start + size
    return data[start:end]


@njit
def neutron_xs_energy_grid_last(nuclide, data):
    start = nuclide["neutron_xs_energy_grid_offset"]
    size = nuclide["neutron_xs_energy_grid_length"]
    end = start + size
    return data[end - 1]


@njit
def neutron_xs_energy_grid_chunk(start, length, nuclide, data):
    start += nuclide["neutron_xs_energy_grid_offset"]
    end = start + length
    return data[start:end]


@njit
def neutron_total_xs(index, nuclide, data):
    offset = nuclide["neutron_total_xs_offset"]
    return data[offset + index]


@njit
def neutron_total_xs_all(nuclide, data):
    start = nuclide["neutron_total_xs_offset"]
    size = nuclide["neutron_total_xs_length"]
    end = start + size
    return data[start:end]


@njit
def neutron_total_xs_last(nuclide, data):
    start = nuclide["neutron_total_xs_offset"]
    size = nuclide["neutron_total_xs_length"]
    end = start + size
    return data[end - 1]


@njit
def neutron_total_xs_chunk(start, length, nuclide, data):
    start += nuclide["neutron_total_xs_offset"]
    end = start + length
    return data[start:end]


@njit
def neutron_elastic_xs(index, nuclide, data):
    offset = nuclide["neutron_elastic_xs_offset"]
    return data[offset + index]


@njit
def neutron_elastic_xs_all(nuclide, data):
    start = nuclide["neutron_elastic_xs_offset"]
    size = nuclide["neutron_elastic_xs_length"]
    end = start + size
    return data[start:end]


@njit
def neutron_elastic_xs_last(nuclide, data):
    start = nuclide["neutron_elastic_xs_offset"]
    size = nuclide["neutron_elastic_xs_length"]
    end = start + size
    return data[end - 1]


@njit
def neutron_elastic_xs_chunk(start, length, nuclide, data):
    start += nuclide["neutron_elastic_xs_offset"]
    end = start + length
    return data[start:end]


@njit
def neutron_capture_xs(index, nuclide, data):
    offset = nuclide["neutron_capture_xs_offset"]
    return data[offset + index]


@njit
def neutron_capture_xs_all(nuclide, data):
    start = nuclide["neutron_capture_xs_offset"]
    size = nuclide["neutron_capture_xs_length"]
    end = start + size
    return data[start:end]


@njit
def neutron_capture_xs_last(nuclide, data):
    start = nuclide["neutron_capture_xs_offset"]
    size = nuclide["neutron_capture_xs_length"]
    end = start + size
    return data[end - 1]


@njit
def neutron_capture_xs_chunk(start, length, nuclide, data):
    start += nuclide["neutron_capture_xs_offset"]
    end = start + length
    return data[start:end]


@njit
def neutron_inelastic_xs(index, nuclide, data):
    offset = nuclide["neutron_inelastic_xs_offset"]
    return data[offset + index]


@njit
def neutron_inelastic_xs_all(nuclide, data):
    start = nuclide["neutron_inelastic_xs_offset"]
    size = nuclide["neutron_inelastic_xs_length"]
    end = start + size
    return data[start:end]


@njit
def neutron_inelastic_xs_last(nuclide, data):
    start = nuclide["neutron_inelastic_xs_offset"]
    size = nuclide["neutron_inelastic_xs_length"]
    end = start + size
    return data[end - 1]


@njit
def neutron_inelastic_xs_chunk(start, length, nuclide, data):
    start += nuclide["neutron_inelastic_xs_offset"]
    end = start + length
    return data[start:end]


@njit
def neutron_fission_xs(index, nuclide, data):
    offset = nuclide["neutron_fission_xs_offset"]
    return data[offset + index]


@njit
def neutron_fission_xs_all(nuclide, data):
    start = nuclide["neutron_fission_xs_offset"]
    size = nuclide["neutron_fission_xs_length"]
    end = start + size
    return data[start:end]


@njit
def neutron_fission_xs_last(nuclide, data):
    start = nuclide["neutron_fission_xs_offset"]
    size = nuclide["neutron_fission_xs_length"]
    end = start + size
    return data[end - 1]


@njit
def neutron_fission_xs_chunk(start, length, nuclide, data):
    start += nuclide["neutron_fission_xs_offset"]
    end = start + length
    return data[start:end]


@njit
def proton_xs_energy_grid(index, nuclide, data):
    offset = nuclide["proton_xs_energy_grid_offset"]
    return data[offset + index]


@njit
def proton_xs_energy_grid_all(nuclide, data):
    start = nuclide["proton_xs_energy_grid_offset"]
    size = nuclide["proton_xs_energy_grid_length"]
    end = start + size
    return data[start:end]


@njit
def proton_xs_energy_grid_last(nuclide, data):
    start = nuclide["proton_xs_energy_grid_offset"]
    size = nuclide["proton_xs_energy_grid_length"]
    end = start + size
    return data[end - 1]


@njit
def proton_xs_energy_grid_chunk(start, length, nuclide, data):
    start += nuclide["proton_xs_energy_grid_offset"]
    end = start + length
    return data[start:end]


@njit
def proton_total_xs(index, nuclide, data):
    offset = nuclide["proton_total_xs_offset"]
    return data[offset + index]


@njit
def proton_total_xs_all(nuclide, data):
    start = nuclide["proton_total_xs_offset"]
    size = nuclide["proton_total_xs_length"]
    end = start + size
    return data[start:end]


@njit
def proton_total_xs_last(nuclide, data):
    start = nuclide["proton_total_xs_offset"]
    size = nuclide["proton_total_xs_length"]
    end = start + size
    return data[end - 1]


@njit
def proton_total_xs_chunk(start, length, nuclide, data):
    start += nuclide["proton_total_xs_offset"]
    end = start + length
    return data[start:end]


@njit
def proton_elastic_xs(index, nuclide, data):
    offset = nuclide["proton_elastic_xs_offset"]
    return data[offset + index]


@njit
def proton_elastic_xs_all(nuclide, data):
    start = nuclide["proton_elastic_xs_offset"]
    size = nuclide["proton_elastic_xs_length"]
    end = start + size
    return data[start:end]


@njit
def proton_elastic_xs_last(nuclide, data):
    start = nuclide["proton_elastic_xs_offset"]
    size = nuclide["proton_elastic_xs_length"]
    end = start + size
    return data[end - 1]


@njit
def proton_elastic_xs_chunk(start, length, nuclide, data):
    start += nuclide["proton_elastic_xs_offset"]
    end = start + length
    return data[start:end]


@njit
def proton_capture_xs(index, nuclide, data):
    offset = nuclide["proton_capture_xs_offset"]
    return data[offset + index]


@njit
def proton_capture_xs_all(nuclide, data):
    start = nuclide["proton_capture_xs_offset"]
    size = nuclide["proton_capture_xs_length"]
    end = start + size
    return data[start:end]


@njit
def proton_capture_xs_last(nuclide, data):
    start = nuclide["proton_capture_xs_offset"]
    size = nuclide["proton_capture_xs_length"]
    end = start + size
    return data[end - 1]


@njit
def proton_capture_xs_chunk(start, length, nuclide, data):
    start += nuclide["proton_capture_xs_offset"]
    end = start + length
    return data[start:end]


@njit
def proton_inelastic_xs(index, nuclide, data):
    offset = nuclide["proton_inelastic_xs_offset"]
    return data[offset + index]


@njit
def proton_inelastic_xs_all(nuclide, data):
    start = nuclide["proton_inelastic_xs_offset"]
    size = nuclide["proton_inelastic_xs_length"]
    end = start + size
    return data[start:end]


@njit
def proton_inelastic_xs_last(nuclide, data):
    start = nuclide["proton_inelastic_xs_offset"]
    size = nuclide["proton_inelastic_xs_length"]
    end = start + size
    return data[end - 1]


@njit
def proton_inelastic_xs_chunk(start, length, nuclide, data):
    start += nuclide["proton_inelastic_xs_offset"]
    end = start + length
    return data[start:end]


@njit
def neutron_elastic_scattering_reaction_IDs(index, nuclide, data):
    offset = nuclide["neutron_elastic_scattering_reaction_IDs_offset"]
    return data[offset + index]


@njit
def neutron_elastic_scattering_reaction_IDs_all(nuclide, data):
    start = nuclide["neutron_elastic_scattering_reaction_IDs_offset"]
    size = nuclide["N_neutron_elastic_scattering_reaction"]
    end = start + size
    return data[start:end]


@njit
def neutron_elastic_scattering_reaction_IDs_last(nuclide, data):
    start = nuclide["neutron_elastic_scattering_reaction_IDs_offset"]
    size = nuclide["N_neutron_elastic_scattering_reaction"]
    end = start + size
    return data[end - 1]


@njit
def neutron_elastic_scattering_reaction_IDs_chunk(start, length, nuclide, data):
    start += nuclide["neutron_elastic_scattering_reaction_IDs_offset"]
    end = start + length
    return data[start:end]


@njit
def neutron_capture_reaction_IDs(index, nuclide, data):
    offset = nuclide["neutron_capture_reaction_IDs_offset"]
    return data[offset + index]


@njit
def neutron_capture_reaction_IDs_all(nuclide, data):
    start = nuclide["neutron_capture_reaction_IDs_offset"]
    size = nuclide["N_neutron_capture_reaction"]
    end = start + size
    return data[start:end]


@njit
def neutron_capture_reaction_IDs_last(nuclide, data):
    start = nuclide["neutron_capture_reaction_IDs_offset"]
    size = nuclide["N_neutron_capture_reaction"]
    end = start + size
    return data[end - 1]


@njit
def neutron_capture_reaction_IDs_chunk(start, length, nuclide, data):
    start += nuclide["neutron_capture_reaction_IDs_offset"]
    end = start + length
    return data[start:end]


@njit
def neutron_inelastic_scattering_reaction_IDs(index, nuclide, data):
    offset = nuclide["neutron_inelastic_scattering_reaction_IDs_offset"]
    return data[offset + index]


@njit
def neutron_inelastic_scattering_reaction_IDs_all(nuclide, data):
    start = nuclide["neutron_inelastic_scattering_reaction_IDs_offset"]
    size = nuclide["N_neutron_inelastic_scattering_reaction"]
    end = start + size
    return data[start:end]


@njit
def neutron_inelastic_scattering_reaction_IDs_last(nuclide, data):
    start = nuclide["neutron_inelastic_scattering_reaction_IDs_offset"]
    size = nuclide["N_neutron_inelastic_scattering_reaction"]
    end = start + size
    return data[end - 1]


@njit
def neutron_inelastic_scattering_reaction_IDs_chunk(start, length, nuclide, data):
    start += nuclide["neutron_inelastic_scattering_reaction_IDs_offset"]
    end = start + length
    return data[start:end]


@njit
def neutron_fission_reaction_IDs(index, nuclide, data):
    offset = nuclide["neutron_fission_reaction_IDs_offset"]
    return data[offset + index]


@njit
def neutron_fission_reaction_IDs_all(nuclide, data):
    start = nuclide["neutron_fission_reaction_IDs_offset"]
    size = nuclide["N_neutron_fission_reaction"]
    end = start + size
    return data[start:end]


@njit
def neutron_fission_reaction_IDs_last(nuclide, data):
    start = nuclide["neutron_fission_reaction_IDs_offset"]
    size = nuclide["N_neutron_fission_reaction"]
    end = start + size
    return data[end - 1]


@njit
def neutron_fission_reaction_IDs_chunk(start, length, nuclide, data):
    start += nuclide["neutron_fission_reaction_IDs_offset"]
    end = start + length
    return data[start:end]


@njit
def proton_elastic_scattering_reaction_IDs(index, nuclide, data):
    offset = nuclide["proton_elastic_scattering_reaction_IDs_offset"]
    return data[offset + index]


@njit
def proton_elastic_scattering_reaction_IDs_all(nuclide, data):
    start = nuclide["proton_elastic_scattering_reaction_IDs_offset"]
    size = nuclide["N_proton_elastic_scattering_reaction"]
    end = start + size
    return data[start:end]


@njit
def proton_elastic_scattering_reaction_IDs_last(nuclide, data):
    start = nuclide["proton_elastic_scattering_reaction_IDs_offset"]
    size = nuclide["N_proton_elastic_scattering_reaction"]
    end = start + size
    return data[end - 1]


@njit
def proton_elastic_scattering_reaction_IDs_chunk(start, length, nuclide, data):
    start += nuclide["proton_elastic_scattering_reaction_IDs_offset"]
    end = start + length
    return data[start:end]


@njit
def proton_capture_reaction_IDs(index, nuclide, data):
    offset = nuclide["proton_capture_reaction_IDs_offset"]
    return data[offset + index]


@njit
def proton_capture_reaction_IDs_all(nuclide, data):
    start = nuclide["proton_capture_reaction_IDs_offset"]
    size = nuclide["N_proton_capture_reaction"]
    end = start + size
    return data[start:end]


@njit
def proton_capture_reaction_IDs_last(nuclide, data):
    start = nuclide["proton_capture_reaction_IDs_offset"]
    size = nuclide["N_proton_capture_reaction"]
    end = start + size
    return data[end - 1]


@njit
def proton_capture_reaction_IDs_chunk(start, length, nuclide, data):
    start += nuclide["proton_capture_reaction_IDs_offset"]
    end = start + length
    return data[start:end]


@njit
def proton_inelastic_scattering_reaction_IDs(index, nuclide, data):
    offset = nuclide["proton_inelastic_scattering_reaction_IDs_offset"]
    return data[offset + index]


@njit
def proton_inelastic_scattering_reaction_IDs_all(nuclide, data):
    start = nuclide["proton_inelastic_scattering_reaction_IDs_offset"]
    size = nuclide["N_proton_inelastic_scattering_reaction"]
    end = start + size
    return data[start:end]


@njit
def proton_inelastic_scattering_reaction_IDs_last(nuclide, data):
    start = nuclide["proton_inelastic_scattering_reaction_IDs_offset"]
    size = nuclide["N_proton_inelastic_scattering_reaction"]
    end = start + size
    return data[end - 1]


@njit
def proton_inelastic_scattering_reaction_IDs_chunk(start, length, nuclide, data):
    start += nuclide["proton_inelastic_scattering_reaction_IDs_offset"]
    end = start + length
    return data[start:end]


@njit
def neutron_fission_delayed_fractions(index, nuclide, data):
    offset = nuclide["neutron_fission_delayed_fractions_offset"]
    return data[offset + index]


@njit
def neutron_fission_delayed_fractions_all(nuclide, data):
    start = nuclide["neutron_fission_delayed_fractions_offset"]
    size = nuclide["neutron_fission_delayed_fractions_length"]
    end = start + size
    return data[start:end]


@njit
def neutron_fission_delayed_fractions_last(nuclide, data):
    start = nuclide["neutron_fission_delayed_fractions_offset"]
    size = nuclide["neutron_fission_delayed_fractions_length"]
    end = start + size
    return data[end - 1]


@njit
def neutron_fission_delayed_fractions_chunk(start, length, nuclide, data):
    start += nuclide["neutron_fission_delayed_fractions_offset"]
    end = start + length
    return data[start:end]


@njit
def neutron_fission_delayed_decay_rates(index, nuclide, data):
    offset = nuclide["neutron_fission_delayed_decay_rates_offset"]
    return data[offset + index]


@njit
def neutron_fission_delayed_decay_rates_all(nuclide, data):
    start = nuclide["neutron_fission_delayed_decay_rates_offset"]
    size = nuclide["neutron_fission_delayed_decay_rates_length"]
    end = start + size
    return data[start:end]


@njit
def neutron_fission_delayed_decay_rates_last(nuclide, data):
    start = nuclide["neutron_fission_delayed_decay_rates_offset"]
    size = nuclide["neutron_fission_delayed_decay_rates_length"]
    end = start + size
    return data[end - 1]


@njit
def neutron_fission_delayed_decay_rates_chunk(start, length, nuclide, data):
    start += nuclide["neutron_fission_delayed_decay_rates_offset"]
    end = start + length
    return data[start:end]


@njit
def neutron_fission_delayed_spectrum_IDs(index, nuclide, data):
    offset = nuclide["neutron_fission_delayed_spectrum_IDs_offset"]
    return data[offset + index]


@njit
def neutron_fission_delayed_spectrum_IDs_all(nuclide, data):
    start = nuclide["neutron_fission_delayed_spectrum_IDs_offset"]
    size = nuclide["N_neutron_fission_delayed_spectrum"]
    end = start + size
    return data[start:end]


@njit
def neutron_fission_delayed_spectrum_IDs_last(nuclide, data):
    start = nuclide["neutron_fission_delayed_spectrum_IDs_offset"]
    size = nuclide["N_neutron_fission_delayed_spectrum"]
    end = start + size
    return data[end - 1]


@njit
def neutron_fission_delayed_spectrum_IDs_chunk(start, length, nuclide, data):
    start += nuclide["neutron_fission_delayed_spectrum_IDs_offset"]
    end = start + length
    return data[start:end]
